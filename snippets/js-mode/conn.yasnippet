# -*- mode: snippet -*-
# name: Redux Connected Component
# key: conn
# --
import { connect } from "react-redux";

import $1 from "../base/$1.js";

const mapStateToProps = state => ({ $2 });

export default connect(mapStateToProps)(
  $1 
);
