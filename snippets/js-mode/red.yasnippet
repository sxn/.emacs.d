# -*- mode: snippet -*-
# name: Redux Reducer
# key: red
# --
const defaultState = {$2}

const $1 = (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default $1;
