# -*- mode: snippet -*-
# name: Angular Service Spec
# key: a2ss
# --

import { TestBed, inject } from '@angular/core/testing';

import { $1Service } from './top-up.service';

describe('$1Service', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [$1Service]
    });
  });

  it('should be created', inject([$1Service], (service: $1Service) => {
    expect(service).toBeTruthy();
  }));
});